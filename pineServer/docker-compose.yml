version: '3.8'

services:
  pineserver:
    build: .
    container_name: pineserver
    ports:
      - "5050:8000"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - pine-network

networks:
  pine-network:
    driver: bridge
